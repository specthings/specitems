SPDX-License-Identifier: CC-BY-SA-4.0 OR BSD-2-Clause
copyrights:
- Copyright (C) 2026 embedded brains GmbH & Co. KG
enabled-by: true
links:
- role: spec-member
  uid: root
- role: spec-refinement
  spec-key: type
  spec-value: tool-config
  uid: root
spec-description: null
spec-example: null
spec-info:
  dict:
    attributes:
      item-cache:
        description: |
          It shall be the item cache of the configuration.
        spec-type: tool-item-cache
      tasks:
        description: |
          It shall be the tasks of the configuration.
        spec-type: tool-task-list
    description: |
      This set of attributes specifies the configuration of the tools.  The
      file specitems.yml of a tree holds one such item.  A tool reads the
      item cache and performs every task of its own type.
    mandatory-attributes: all
spec-name: Tool Configuration Item Type
spec-type: tool-config
type: spec
